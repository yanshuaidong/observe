```json
{
  "strategyId": "string (YYYYMMDD)",
  "content": {

    "strategyMeta": {
      "strategyName": "string",
      "strategyDescription": "string",
      "strategyStatus": "string (enum: observing | paused | ended)",
      "createDate": "string (YYYYMMDD)"
    },

    "records": [
      {
        "id": "number (timestamp)",
        "date": "string (YYYYMMDD)",
        "time": "string (YYYY-MM-DD HH:mm)",

        "ideaContent": {

          "varietyAndOperation": {
            "varietyName": "string",
            "contractCode": "string | null",
            "direction": "string (enum: long | short | wait_and_see)",
            "operationType": "string (enum: no_action | open | close | add | reduce | stop_loss | take_profit)",
            "operationPrice": "number | null",
            "operationQuantity": "number | null",
            "stopLossPrice": "number | null",
            "takeProfitPrice": "number | null",
            "operationReason": "string | null",
            "hasRegret": "boolean",
            "regretReason": "string | null"
          },

          "source": {
            "attentionSource": "string (enum: xinhua_news | guotaijunan_news | cailian_news | eastmoney_info | bloomberg_news | kline_feeling | friend_recommendation | sudden_idea | other)",
            "attentionSourceDetail": "string | null"
          },

          "technical": {
            "isVolumeUp": "boolean",
            "isOpenInterestUp": "boolean",
            "isPriceUp": "boolean",
            "currentPrice": "number | null",
            "supportLevel": "number | null",
            "resistanceLevel": "number | null",
            "technicalNote": "string | null"
          },

          "fundamental": [
            {
              "organization": "string",
              "opinion": "string"
            }
          ],

          "news": [
            {
              "message": "string",
              "source": "string"
            }
          ],

          "sentiment": {
            "myEmotion": "string | null (enum: greed | fear | hesitation | calm | excitement | anxiety | indifferent)",
            "emotionIntensity": "number | null (1-5)",
            "marketSentiment": "string | null (enum: bullish | panic | volatile | wait_and_see | unknown)",
            "sentimentNote": "string | null"
          }

        }
      }
    ],

    "strategySummary": [
      {
        "summaryDate": "string (YYYYMMDD)",
        "summaryPeriod": "string (enum: weekly | monthly | strategy_end)",
        "observationDays": "number",
        "triggerCount": "number",
        "actualOperationCount": "number",
        "profitLossNote": "string | null",
        "winRateNote": "string | null",
        "lessonsLearned": "string | null",
        "needAdjustment": "boolean",
        "adjustmentNote": "string | null"
      }
    ]

  }
}
```
